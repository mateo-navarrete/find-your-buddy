const initState = {
  animals: [],
};

export const animalsReducer = (state = initState, action) => {
  // let nextState;
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state;
  }
};
